# Security Policy

Report security-sensitive issues through this repository's **Security → Report a
vulnerability** private GitHub advisory flow rather than a public issue.

Include the affected commit, route or generated asset, browser, reproduction,
and expected impact. Do not include real credentials or personal data. Issues
owned by the compiler, Web runtime, CLI, or production verifier should be
reported privately to the VelarScript repository instead.
