# @velarscript/site-ui

Private VelarScript brand and marketing-site component package. It composes the
theme-neutral `@velarscript/ui` primitives with the VelarScript palette, typography,
spacing, surfaces, and brand mark.

This package is intentionally website-specific and is not part of the Web
framework API.
